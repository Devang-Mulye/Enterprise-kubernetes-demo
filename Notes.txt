This file will contain random notes and other things which might be useful later on -

=== Jib open-source link ===
https://github.com/GoogleContainerTools/jib

= Config cache

=== Docker Commands ===
docker build -t k8s-demo/openjdk17:1.0 .
docker run k8s-demo/openjdk17:1.0

=== kubernetes
jibDockerBuild - incompatible with config cache???
Because it has some systems which do not work with gradle 9+

=== suggestion - use jib ===

=== Helm ===
Use helm template more

nerdctl -n k8s.io images
docker save k8s-demo/enterprise-kubernetes-demo:1.0 -o image.tar
nerdctl -n k8s.io load -i image.tar
nerdctl -n k8s.io images

helm install enterprise-kubernetes-demo ./helm-charts -n k8s-demo
helm uninstall enterprise-kubernetes-demo -n k8s-demo


Steps -
1. Create a new docker image
2. Build our local image using this docker as base
3. Uninstall existing k8s image
4. Deploy the image to kubernetes
5. Verify changes

