replicaCount: 1

image:
  repository: k8s-demo/enterprise-kubernetes-demo
  tag: "1.0"
  # Use local image - never pull from dockerhub
  pullPolicy: Never

nameOverride: "Helm Charts"
fullnameOverride: "Helm Charts"

container:
  port: 8080

service:
  type: ClusterIP
  port: 80
